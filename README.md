# Store Intelligence API

Containerized retail analytics API that ingests CCTV-derived visitor events and exposes live store metrics.

## Setup

```bash
git clone <repo-url>
cd store-intelligence
docker compose up --build